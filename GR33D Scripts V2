-- ==========================================
-- GR33D SCRIPTS V10.0 (UPDATED - SPEED, JUMP & TP)
-- Speed: Delta-time CFrame movement (No rubberbanding)
-- Jump: State-forced Impulse (High Jump support)
-- Fly: Camera-relative via PlayerModule controls
-- TP: Ground Raycast + Faster Step-TP (Distance: 25, Wait: 0.015)
-- UI: Right side
-- ==========================================

local UserInputService = game:GetService("UserInputService")
local RunService       = game:GetService("RunService")
local Players          = game:GetService("Players")

local player = Players.LocalPlayer
local protectedParent = player:WaitForChild("PlayerGui")
if protectedParent:FindFirstChild("Panel") then
    protectedParent.Panel:Destroy()
end

local camera = workspace.CurrentCamera

-- ==========================================
-- LOGGER
-- ==========================================

local LOG_FILE_NAME   = "gr33d_log.txt"
local logBuffer       = {}
local logStartTime    = os.clock()
local MAX_LOG_LINES   = 5000
local logCount        = 0

local function log(cat, msg)
    if logCount >= MAX_LOG_LINES then return end
    logCount = logCount + 1
    local t = string.format("%.3f", os.clock() - logStartTime)
    local line = string.format("[%s][%s] %s", t, cat, msg)
    table.insert(logBuffer, line)
    print(line)
end

local function saveLogToFile()
    if writefile then
        pcall(function() writefile(LOG_FILE_NAME, table.concat(logBuffer, "\n")) end)
    end
end

log("BOOT", "=== GR33D V10.0 Starting ===")
log("BOOT", "PlaceId: " .. tostring(game.PlaceId))
log("BOOT", "Player: " .. player.Name)

-- ==========================================
-- CONFIG
-- ==========================================

local MAX_SPEED_INPUT   = 1000
local DEFAULT_SPEED     = 100

-- Fly
local FLY_SPEED         = 120       -- studs/s
local DOUBLE_TAP_WINDOW = 0.35

-- Jump
local MAX_JUMP_IMPULSE  = 500
local DEFAULT_JUMP      = 100
local MAX_SAFE_FALL     = 50

-- Prompt Extender
local PROMPT_MAX_DISTANCE  = 50
local PROMPT_HOLD_DURATION = 0
local PROMPT_REQUIRES_LOS  = false

-- ==========================================
-- STATE
-- ==========================================

local flyToggleEnabled      = false
local isFlying              = false
local invisibleEnabled      = false
local noFallDamageEnabled   = false
local noclipEnabled         = false
local runSpeedEnabled       = false
local jumpEnabled           = false
local promptExtenderEnabled = false
local isMinimized           = false

local runSpeedValue         = DEFAULT_SPEED
local jumpPowerValue        = DEFAULT_JUMP
local teleportRiskyMode     = false
local maxFrameStep          = 10
local originalWalkSpeed     = 16

local lastJumpTapTime       = 0
local lastJumpReqTime       = 0
local realSpawnCFrame       = nil
local firstSpawnCaptured    = false
local savedMapCFrame        = nil

local character, rootPart, humanoid
local attachment, linearVelocity
local speedAttachment, speedLinearVelocity

local originalTransparency      = {}
local originalCollision         = {}
local invisibilityHookInstalled = false
local invisConnection           = nil
local oldNewIndex               = nil

local isTPBusy              = false
local trackedPrompts        = {}
local observeCheatMode      = false
local lastObservedPos       = nil
local lastObservedTick      = 0
local autoRotateWasEnabled  = true

local function zeroCharacterMotion()
    if not rootPart or not rootPart.Parent then return end
    rootPart.AssemblyLinearVelocity = Vector3.zero
    rootPart.AssemblyAngularVelocity = Vector3.zero
    if humanoid then
        humanoid.AutoRotate = false
        pcall(function()
            humanoid:Move(Vector3.zero, false)
        end)
    end
end

local function neutralizeCharacterPhysics()
    if not rootPart or not rootPart.Parent then return end
    rootPart.AssemblyLinearVelocity = Vector3.zero
    rootPart.AssemblyAngularVelocity = Vector3.zero
    if humanoid then
        humanoid.PlatformStand = true
        humanoid.AutoRotate = false
        humanoid.WalkSpeed = 0
        pcall(function()
            humanoid:Move(Vector3.zero, false)
        end)
    end
end

local function stabilizeRoot(targetCFrame)
    if not rootPart or not rootPart.Parent then return end
    rootPart.CFrame = targetCFrame
    zeroCharacterMotion()
end

local function freezeCharacterForTeleport()
    if not humanoid or not rootPart then return end
    humanoid.Sit = false
    humanoid.PlatformStand = true
    humanoid.AutoRotate = false
    humanoid.WalkSpeed = 0
    rootPart.Anchored = true
    zeroCharacterMotion()
end

local function releaseCharacterAfterTeleport()
    if not humanoid or not rootPart then return end
    rootPart.AssemblyLinearVelocity = Vector3.zero
    rootPart.AssemblyAngularVelocity = Vector3.zero
    rootPart.Anchored = false
    humanoid.PlatformStand = false
    humanoid.AutoRotate = true
    humanoid.WalkSpeed = originalWalkSpeed
end

local function logPhysicsSnapshot(label)
    if not rootPart or not humanoid then return end
    local pos = rootPart.Position
    local vel = rootPart.AssemblyLinearVelocity
    local ang = rootPart.AssemblyAngularVelocity
    local state = humanoid:GetState()
    local moveDir = humanoid.MoveDirection
    local delta = 0
    if lastObservedPos then
        delta = (pos - lastObservedPos).Magnitude
    end
    log("OBS", string.format("%s | pos=(%.2f, %.2f, %.2f) vel=(%.2f, %.2f, %.2f) ang=(%.2f, %.2f, %.2f) ws=%.1f state=%s move=(%.2f, %.2f, %.2f) delta=%.3f anchored=%s",
        label,
        pos.X, pos.Y, pos.Z,
        vel.X, vel.Y, vel.Z,
        ang.X, ang.Y, ang.Z,
        humanoid.WalkSpeed,
        tostring(state),
        moveDir.X, moveDir.Y, moveDir.Z,
        delta,
        tostring(rootPart.Anchored)
    ))
    lastObservedPos = pos
    lastObservedTick = os.clock()
end

local function startCheatObserver()
    if not observeCheatMode then return end
    if not rootPart or not humanoid then return end
    local now = os.clock()
    if now - lastObservedTick < 0.25 then return end
    logPhysicsSnapshot("CHEAT_OBSERVER")
end

-- Fly controls module
local controls              = nil
pcall(function()
    local playerScripts = player:WaitForChild("PlayerScripts", 5)
    if playerScripts then
        local playerModuleScript = playerScripts:WaitForChild("PlayerModule", 5)
        if playerModuleScript then
            local PlayerModule = require(playerModuleScript)
            controls = PlayerModule:GetControls()
        end
    end
end)

-- ==========================================
-- INVISIBILITY
-- ==========================================

local function installInvisibilityHook()
    if invisibilityHookInstalled then return end
    invisibilityHookInstalled = true
    pcall(function()
        local mt = getrawmetatable(game)
        oldNewIndex = hookfunction(mt.__newindex, newcclosure(function(t, k, v)
            if not checkcaller() and typeof(t) == "Instance" and t:IsA("BasePart") then
                local char = player.Character
                if char and t:IsDescendantOf(char) and invisibleEnabled then
                    if k == "Transparency" and v < 1 then return end
                    if k == "LocalTransparencyModifier" and v < 1 then return end
                end
            end
            return oldNewIndex(t, k, v)
        end))
    end)
end

local function applyInvisibility(state)
    invisibleEnabled = state
    log("INVIS", "Invisibility: " .. tostring(state))
    if not character then return end
    if state then
        installInvisibilityHook()
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA("BasePart") or part:IsA("Decal") then
                if not originalTransparency[part] then
                    originalTransparency[part] = part.Transparency
                end
                part.Transparency = 1
                part.LocalTransparencyModifier = 1
            end
        end
    else
        for part, orig in pairs(originalTransparency) do
            if part and part.Parent then
                part.Transparency = orig
                part.LocalTransparencyModifier = 0
            end
        end
        originalTransparency = {}
    end
end

local function watchCharacterForInvisibility(char)
    if invisConnection then invisConnection:Disconnect() end
    invisConnection = char.DescendantAdded:Connect(function(desc)
        if invisibleEnabled and (desc:IsA("BasePart") or desc:IsA("Decal")) then
            originalTransparency[desc] = desc.Transparency
            desc.Transparency = 1
            desc.LocalTransparencyModifier = 1
        end
    end)
end

-- ==========================================
-- PROMPT EXTENDER
-- ==========================================

local function applyPromptExtender(prompt)
    if not prompt or not prompt:IsA("ProximityPrompt") then return end
    if trackedPrompts[prompt] then return end
    trackedPrompts[prompt] = {
        MaxActivationDistance = prompt.MaxActivationDistance,
        HoldDuration          = prompt.HoldDuration,
        RequiresLineOfSight   = prompt.RequiresLineOfSight,
    }
    pcall(function()
        prompt.MaxActivationDistance = PROMPT_MAX_DISTANCE
        prompt.HoldDuration          = PROMPT_HOLD_DURATION
        prompt.RequiresLineOfSight   = PROMPT_REQUIRES_LOS
    end)
end

local function removePromptExtender(prompt)
    local saved = trackedPrompts[prompt]
    if not saved then return end
    pcall(function()
        if prompt and prompt.Parent then
            prompt.MaxActivationDistance = saved.MaxActivationDistance
            prompt.HoldDuration          = saved.HoldDuration
            prompt.RequiresLineOfSight   = saved.RequiresLineOfSight
        end
    end)
    trackedPrompts[prompt] = nil
end

local function enablePromptExtenderAll()
    local count = 0
    for _, obj in ipairs(workspace:GetDescendants()) do
        if obj:IsA("ProximityPrompt") then
            applyPromptExtender(obj)
            count = count + 1
        end
    end
    log("PROMPT", "Extended " .. count .. " prompts")
end

local function disablePromptExtenderAll()
    for prompt, _ in pairs(trackedPrompts) do
        removePromptExtender(prompt)
    end
    trackedPrompts = {}
    log("PROMPT", "Disabled")
end

workspace.DescendantAdded:Connect(function(obj)
    if promptExtenderEnabled and obj:IsA("ProximityPrompt") then
        applyPromptExtender(obj)
    end
end)

-- ==========================================
-- NOCLIP
-- ==========================================

local function applyNoclip(state)
    noclipEnabled = state
    if not character then return end
    if state then
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA("BasePart") then
                if originalCollision[part] == nil then
                    originalCollision[part] = part.CanCollide
                end
                part.CanCollide = false
            end
        end
    else
        for part, orig in pairs(originalCollision) do
            if part and part.Parent then
                part.CanCollide = orig
            end
        end
        originalCollision = {}
    end
end

RunService.Stepped:Connect(function()
    if not character or not noclipEnabled then return end
    for _, part in ipairs(character:GetDescendants()) do
        if part:IsA("BasePart") and part.CanCollide then
            if originalCollision[part] == nil then
                originalCollision[part] = true
            end
            part.CanCollide = false
        end
    end
end)

-- ==========================================
-- SPEED (CFrame Offset - Anti-Rubberband)
-- ==========================================

RunService.RenderStepped:Connect(function(dt)
    if not humanoid or not rootPart then return end

    if speedLinearVelocity then
        if not runSpeedEnabled or isFlying or isTPBusy then
            speedLinearVelocity.MaxForce = 0
            speedLinearVelocity.VectorVelocity = Vector3.zero
            humanoid.AutoRotate = autoRotateWasEnabled
        else
            local st = humanoid:GetState()
            if st == Enum.HumanoidStateType.Dead
                or st == Enum.HumanoidStateType.Ragdoll
                or st == Enum.HumanoidStateType.Climbing
                or st == Enum.HumanoidStateType.Swimming
                or humanoid.Sit then
                speedLinearVelocity.MaxForce = 0
                speedLinearVelocity.VectorVelocity = Vector3.zero
                humanoid.AutoRotate = autoRotateWasEnabled
            else
                humanoid.WalkSpeed = 0
                humanoid.AutoRotate = false
                autoRotateWasEnabled = true

                local moveDir = humanoid.MoveDirection
                if moveDir.Magnitude > 0 then
                    local steer = Vector3.new(moveDir.X, 0, moveDir.Z)
                    local desiredSpeed = math.max(runSpeedValue, 1)
                    local targetVel = steer * (desiredSpeed * 12)
                    speedLinearVelocity.MaxForce = 750000
                    speedLinearVelocity.VectorVelocity = targetVel
                    rootPart.AssemblyLinearVelocity = Vector3.zero
                    rootPart.AssemblyAngularVelocity = Vector3.zero
                    rootPart.CFrame = rootPart.CFrame + (steer * math.max(0.4, desiredSpeed * 0.12) * dt * 60)
                    pcall(function()
                        humanoid:Move(Vector3.zero, false)
                    end)
                    if observeCheatMode then
                        logPhysicsSnapshot("SPEED_MOVE")
                    end
                else
                    speedLinearVelocity.MaxForce = 0
                    speedLinearVelocity.VectorVelocity = Vector3.zero
                end
            end
        end
    end

    if observeCheatMode and not runSpeedEnabled then
        startCheatObserver()
    end
end)

-- ==========================================
-- JUMP (Direct Impulse)
-- ==========================================

local function applyJump()
    if not jumpEnabled or not rootPart or not humanoid then return end

    local state = humanoid:GetState()
    if state == Enum.HumanoidStateType.Freefall or state == Enum.HumanoidStateType.FallingDown then 
        return 
    end

    if os.clock() - lastJumpReqTime < 0.15 then return end
    lastJumpReqTime = os.clock()

    humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
    local impulse = math.min(jumpPowerValue, MAX_JUMP_IMPULSE)
    rootPart.AssemblyLinearVelocity = Vector3.new(
        rootPart.AssemblyLinearVelocity.X,
        impulse,
        rootPart.AssemblyLinearVelocity.Z
    )
end

-- ==========================================
-- ADVANCED TELEPORT SYSTEM
-- ==========================================

local function getSafeGroundPosition(targetCFrame)
    local origin = targetCFrame.Position + Vector3.new(0, 15, 0)
    local raycastParams = RaycastParams.new()
    raycastParams.FilterType = Enum.RaycastFilterType.Exclude
    if character then
        raycastParams.FilterDescendantsInstances = {character}
    end
    
    local rayResult = workspace:Raycast(origin, Vector3.new(0, -60, 0), raycastParams)
    if rayResult then
        return CFrame.new(rayResult.Position + Vector3.new(0, 3.5, 0)) * targetCFrame.Rotation
    end
    return targetCFrame + Vector3.new(0, 3, 0)
end

local function absoluteTeleport(targetCFrame, useInterpolation)
    if not character or not rootPart or not humanoid then return end
    if isTPBusy then return end
    isTPBusy = true

    local safeCFrame = getSafeGroundPosition(targetCFrame)
    local startCFrame = rootPart.CFrame
    local distance = (safeCFrame.Position - startCFrame.Position).Magnitude
    local riskyMode = teleportRiskyMode or (useInterpolation == false)

    log("TP", string.format("Starting TP | Mode: %s | Distance: %.1f studs | Risky: %s",
        riskyMode and "Risky" or "Safe", distance, tostring(riskyMode)))
    if observeCheatMode then
        logPhysicsSnapshot("TP_BEFORE")
    end

    local wasNoFall = noFallDamageEnabled
    noFallDamageEnabled = true
    freezeCharacterForTeleport()

    if riskyMode then
        character:PivotTo(safeCFrame)
        task.wait(0.02)
    else
        local stepDistance = 30
        local stepWait = 0.01
        local steps = math.max(math.ceil(distance / stepDistance), 4)

        for i = 1, steps do
            if not rootPart or not rootPart.Parent then break end
            local alpha = i / steps
            local nextCFrame = startCFrame:Lerp(safeCFrame, alpha)
            character:PivotTo(nextCFrame)
            zeroCharacterMotion()
            task.wait(stepWait)
        end
    end

    character:PivotTo(safeCFrame)
    rootPart.CFrame = safeCFrame
    rootPart.AssemblyLinearVelocity = Vector3.zero
    rootPart.AssemblyAngularVelocity = Vector3.zero
    task.wait(0.04)

    if observeCheatMode then
        logPhysicsSnapshot("TP_AFTER")
    end

    task.spawn(function()
        for _ = 1, 12 do
            if rootPart and rootPart.Parent then
                rootPart.CFrame = safeCFrame
                rootPart.AssemblyLinearVelocity = Vector3.zero
                rootPart.AssemblyAngularVelocity = Vector3.zero
                if humanoid then
                    humanoid.WalkSpeed = 0
                    humanoid.AutoRotate = false
                    pcall(function()
                        humanoid:Move(Vector3.zero, false)
                    end)
                end
            end
            task.wait(0.035)
        end

        releaseCharacterAfterTeleport()
        task.delay(0.5, function()
            noFallDamageEnabled = wasNoFall
        end)

        task.delay(0.7, function()
            isTPBusy = false
        end)
    end)
end

-- ==========================================
-- SPAWN LOOKUP
-- ==========================================

local function getEntrySpawn()
    if player.RespawnLocation and player.RespawnLocation:IsA("SpawnLocation") then
        return player.RespawnLocation.CFrame
    end

    local bestSpawn = nil
    for _, obj in ipairs(workspace:GetDescendants()) do
        if obj:IsA("SpawnLocation") and obj.Enabled then
            if player.Team and obj.Neutral == false then
                if obj.TeamColor == player.TeamColor then
                    return obj.CFrame
                end
            else
                bestSpawn = obj.CFrame
            end
        end
    end

    if bestSpawn then return bestSpawn end
    if realSpawnCFrame then return realSpawnCFrame end

    return (rootPart and rootPart.CFrame) or CFrame.new(0, 10, 0)
end

-- ==========================================
-- FLY (camera-relative via PlayerModule)
-- ==========================================

local function setFlying(state)
    if not rootPart or not humanoid or not linearVelocity then return end
    isFlying = state

    if isFlying then
        linearVelocity.MaxForce = 100000
        humanoid:ChangeState(Enum.HumanoidStateType.Freefall)
        log("FLY", "Flight ON")
    else
        linearVelocity.MaxForce = 0
        linearVelocity.VectorVelocity = Vector3.zero
        if humanoid.FloorMaterial ~= Enum.Material.Air then
            humanoid:ChangeState(Enum.HumanoidStateType.Running)
        end
        log("FLY", "Flight OFF")
    end
end

RunService.RenderStepped:Connect(function()
    if not camera or not camera.Parent then
        camera = workspace.CurrentCamera
        return
    end

    if isFlying and linearVelocity and humanoid and rootPart then
        if humanoid.FloorMaterial ~= Enum.Material.Air and humanoid.MoveDirection.Magnitude < 0.1 then
            setFlying(false)
        elseif controls then
            local moveVector = controls:GetMoveVector()
            if moveVector.Magnitude > 0 then
                local camCFrame = camera.CFrame
                local flightDir = (camCFrame.RightVector * moveVector.X)
                                - (camCFrame.LookVector * moveVector.Z)
                linearVelocity.VectorVelocity = flightDir.Unit * FLY_SPEED
            else
                linearVelocity.VectorVelocity = Vector3.zero
            end
        end
    end

    if humanoid and rootPart and noFallDamageEnabled then
        local st = humanoid:GetState()
        if st == Enum.HumanoidStateType.Freefall or st == Enum.HumanoidStateType.FallingDown then
            local v = rootPart.AssemblyLinearVelocity
            if v.Y < -MAX_SAFE_FALL then
                rootPart.AssemblyLinearVelocity = Vector3.new(v.X, -MAX_SAFE_FALL, v.Z)
            end
        end
    end
end)

-- ==========================================
-- UI
-- ==========================================

local function makeDraggable(gui)
    local dragging = false
    local startPos = nil
    local startGuiPos = nil

    gui.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            startPos = input.Position
            startGuiPos = gui.Position
        end
    end)

    gui.InputChanged:Connect(function(input)
        if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
            local delta = input.Position - startPos
            gui.Position = UDim2.new(
                startGuiPos.X.Scale,
                startGuiPos.X.Offset + delta.X,
                startGuiPos.Y.Scale,
                startGuiPos.Y.Offset + delta.Y
            )
        end
    end)

    gui.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = false
            startPos = nil
            startGuiPos = nil
        end
    end)
end

local screenGui = Instance.new("ScreenGui")
screenGui.Name = "Panel"
screenGui.ResetOnSpawn = false
screenGui.IgnoreGuiInset = false
screenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
screenGui.Parent = protectedParent

local mainFrame = Instance.new("Frame")
mainFrame.Name = "MainFrame"
mainFrame.Size = UDim2.new(0, 220, 0, 440)
mainFrame.AnchorPoint = Vector2.new(1, 0.5)
mainFrame.Position = UDim2.new(1, -10, 0.5, 0)
mainFrame.BackgroundColor3 = Color3.fromRGB(18, 18, 22)
mainFrame.Active = true
mainFrame.Draggable = true
mainFrame.Parent = screenGui
makeDraggable(mainFrame)

Instance.new("UICorner", mainFrame).CornerRadius = UDim.new(0, 12)
local stroke = Instance.new("UIStroke", mainFrame)
stroke.Color = Color3.fromRGB(45, 45, 55)
stroke.Thickness = 1.5
stroke.Parent = mainFrame

local topBar = Instance.new("Frame", mainFrame)
topBar.Size = UDim2.new(1, 0, 0, 36)
topBar.Position = UDim2.new(0, 0, 0, 0)
topBar.BackgroundTransparency = 1
topBar.Active = true

local title = Instance.new("TextLabel", topBar)
title.Size = UDim2.new(1, -50, 1, 0)
title.Position = UDim2.new(0, 10, 0, 0)
title.BackgroundTransparency = 1
title.Text = "GR33D v10.0"
title.TextColor3 = Color3.fromRGB(240, 240, 245)
title.Font = Enum.Font.GothamBold
title.TextSize = 13
title.TextXAlignment = Enum.TextXAlignment.Left

local minBtn = Instance.new("TextButton", topBar)
minBtn.Name = "MinBtn"
minBtn.Size = UDim2.new(0, 22, 0, 22)
minBtn.Position = UDim2.new(1, -28, 0.5, -11)
minBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 38)
minBtn.Text = "-"
minBtn.TextColor3 = Color3.fromRGB(200, 200, 210)
minBtn.Font = Enum.Font.GothamBold
minBtn.TextSize = 14
minBtn.ZIndex = 5
Instance.new("UICorner", minBtn).CornerRadius = UDim.new(0, 6)

local container = Instance.new("ScrollingFrame", mainFrame)
container.Size = UDim2.new(1, 0, 1, -40)
container.Position = UDim2.new(0, 0, 0, 40)
container.BackgroundTransparency = 1
container.BorderSizePixel = 0
container.CanvasSize = UDim2.new(0, 0, 0, 720)
container.ScrollBarThickness = 3
container.ScrollBarImageColor3 = Color3.fromRGB(60, 60, 75)

local floatBtn = Instance.new("TextButton")
floatBtn.Name = "FloatBtn"
floatBtn.Size = UDim2.new(0, 36, 0, 36)
floatBtn.AnchorPoint = Vector2.new(1, 0.5)
floatBtn.Position = UDim2.new(1, -10, 0.5, 0)
floatBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
floatBtn.Text = "M"
floatBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
floatBtn.Font = Enum.Font.GothamBold
floatBtn.TextSize = 16
floatBtn.Visible = false
Instance.new("UICorner", floatBtn).CornerRadius = UDim.new(0, 8)
floatBtn.Parent = screenGui
makeDraggable(floatBtn)

local function createToggle(name, y, cb)
    local row = Instance.new("Frame", container)
    row.Size = UDim2.new(1, -8, 0, 28)
    row.Position = UDim2.new(0, 4, 0, y)
    row.BackgroundTransparency = 1

    local lbl = Instance.new("TextLabel", row)
    lbl.Size = UDim2.new(0.65, 0, 1, 0)
    lbl.BackgroundTransparency = 1
    lbl.Text = name
    lbl.TextColor3 = Color3.fromRGB(200, 200, 210)
    lbl.Font = Enum.Font.GothamMedium
    lbl.TextSize = 10
    lbl.TextXAlignment = Enum.TextXAlignment.Left

    local sw = Instance.new("TextButton", row)
    sw.Size = UDim2.new(0, 32, 0, 16)
    sw.Position = UDim2.new(1, -32, 0.5, -8)
    sw.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
    sw.Text = ""
    sw.AutoButtonColor = false
    Instance.new("UICorner", sw).CornerRadius = UDim.new(1, 0)

    local knob = Instance.new("Frame", sw)
    knob.Size = UDim2.new(0, 10, 0, 10)
    knob.Position = UDim2.new(0, 3, 0.5, -5)
    knob.BackgroundColor3 = Color3.fromRGB(200, 200, 210)
    Instance.new("UICorner", knob).CornerRadius = UDim.new(1, 0)

    local a = false
    sw.MouseButton1Click:Connect(function()
        a = not a
        sw.BackgroundColor3 = a and Color3.fromRGB(46, 204, 113) or Color3.fromRGB(40, 40, 50)
        knob.BackgroundColor3 = a and Color3.fromRGB(255, 255, 255) or Color3.fromRGB(200, 200, 210)
        knob:TweenPosition(a and UDim2.new(1, -13, 0.5, -5) or UDim2.new(0, 3, 0.5, -5),
            Enum.EasingDirection.Out, Enum.EasingStyle.Quad, 0.15, true)
        cb(a)
    end)
end

local function createInput(name, y, def, cb)
    local row = Instance.new("Frame", container)
    row.Size = UDim2.new(1, -8, 0, 28)
    row.Position = UDim2.new(0, 4, 0, y)
    row.BackgroundTransparency = 1

    local lbl = Instance.new("TextLabel", row)
    lbl.Size = UDim2.new(0.55, 0, 1, 0)
    lbl.BackgroundTransparency = 1
    lbl.Text = name
    lbl.TextColor3 = Color3.fromRGB(200, 200, 210)
    lbl.Font = Enum.Font.GothamMedium
    lbl.TextSize = 10
    lbl.TextXAlignment = Enum.TextXAlignment.Left

    local box = Instance.new("TextBox", row)
    box.Size = UDim2.new(0, 55, 0, 20)
    box.Position = UDim2.new(1, -55, 0.5, -10)
    box.BackgroundColor3 = Color3.fromRGB(30, 30, 38)
    box.Text = tostring(def)
    box.TextColor3 = Color3.fromRGB(255, 255, 255)
    box.Font = Enum.Font.GothamBold
    box.TextSize = 10
    Instance.new("UICorner", box).CornerRadius = UDim.new(0, 4)

    box.FocusLost:Connect(function()
        local n = tonumber(box.Text)
        if n then cb(n, box) else box.Text = tostring(def) end
    end)
end

local function createButton(name, y, color, cb)
    local b = Instance.new("TextButton", container)
    b.Size = UDim2.new(1, -8, 0, 26)
    b.Position = UDim2.new(0, 4, 0, y)
    b.BackgroundColor3 = color
    b.Text = name
    b.TextColor3 = Color3.fromRGB(255, 255, 255)
    b.Font = Enum.Font.GothamBold
    b.TextSize = 10
    Instance.new("UICorner", b).CornerRadius = UDim.new(0, 5)
    b.MouseButton1Click:Connect(cb)
    return b
end

createToggle("Flight Mode", 5, function(s)
    flyToggleEnabled = s
    if not s then
        setFlying(false)
    elseif humanoid and rootPart and linearVelocity then
        setFlying(true)
    end
end)

createToggle("Invisibility", 35, function(s) applyInvisibility(s) end)
createToggle("No Fall Damage", 65, function(s) noFallDamageEnabled = s end)
createToggle("Noclip", 95, function(s) applyNoclip(s) end)

createToggle("Super Speed", 125, function(s) runSpeedEnabled = s end)

createInput("Speed (max 1000)", 155, DEFAULT_SPEED, function(v, box)
    if v > MAX_SPEED_INPUT then v = MAX_SPEED_INPUT end
    if v < 0 then v = 0 end
    runSpeedValue = v
    if box then box.Text = tostring(v) end
end)

createToggle("Custom Jump", 185, function(s) jumpEnabled = s end)

createInput("Jump (max 500)", 215, DEFAULT_JUMP, function(v, box)
    if v > MAX_JUMP_IMPULSE then v = MAX_JUMP_IMPULSE end
    if v < 1 then v = 1 end
    jumpPowerValue = v
    if box then box.Text = tostring(v) end
end)

createInput("Fly Speed", 245, FLY_SPEED, function(v, box)
    if v < 10 then v = 10 end
    if v > 500 then v = 500 end
    FLY_SPEED = v
    if box then box.Text = tostring(v) end
end)

createToggle("Prompt Extender", 275, function(s)
    promptExtenderEnabled = s
    if s then enablePromptExtenderAll() else disablePromptExtenderAll() end
end)

local tpRiskyToggle = createToggle("TP Risky Mode", 305, function(s)
    teleportRiskyMode = s
    log("UI", "Teleport mode changed to " .. (teleportRiskyMode and "Risky" or "Safe"))
end)

createToggle("Observe Cheat", 335, function(s)
    observeCheatMode = s
    log("UI", "Cheat observer " .. (observeCheatMode and "enabled" or "disabled"))
end)

createButton("TP to Spawn", 365, Color3.fromRGB(41, 128, 185), function()
    log("UI", "TP to Spawn Executed")
    absoluteTeleport(getEntrySpawn(), not teleportRiskyMode)
end)

local saveBtn = createButton("Save Location", 395, Color3.fromRGB(142, 68, 173), function()
    if not rootPart then return end
    savedMapCFrame = rootPart.CFrame
    log("UI", string.format("Saved Location: (%.1f, %.1f, %.1f)", 
        rootPart.Position.X, rootPart.Position.Y, rootPart.Position.Z))
    saveBtn.Text = "Saved!"
    task.delay(1.5, function() saveBtn.Text = "Save Location" end)
end)

createButton("TP to Saved", 425, Color3.fromRGB(39, 174, 96), function()
    if savedMapCFrame then
        log("UI", "TP to Saved Executed")
        absoluteTeleport(savedMapCFrame, not teleportRiskyMode)
    else
        log("UI", "TP to Saved Failed: No location saved")
    end
end)

createButton("SAVE LOG", 455, Color3.fromRGB(200, 80, 80), function()
    saveLogToFile()
    log("LOGGER", "Log saved")
end)

minBtn.MouseButton1Click:Connect(function()
    mainFrame.Visible = false
    floatBtn.Visible = true
end)

floatBtn.MouseButton1Click:Connect(function()
    mainFrame.Visible = true
    floatBtn.Visible = false
end)

-- ==========================================
-- JUMP INPUT
-- ==========================================

UserInputService.JumpRequest:Connect(function()
    local now = os.clock()
    if flyToggleEnabled and (now - lastJumpTapTime) <= DOUBLE_TAP_WINDOW then
        if isFlying then setFlying(false)
        elseif humanoid and (humanoid:GetState() == Enum.HumanoidStateType.Freefall
            or humanoid:GetState() == Enum.HumanoidStateType.Jumping) then
            setFlying(true)
        end
        lastJumpTapTime = 0
    else
        lastJumpTapTime = now
    end
    if jumpEnabled and not flyToggleEnabled then
        applyJump()
    end
end)

-- ==========================================
-- CHARACTER SETUP
-- ==========================================

local function setupCharacter(char)
    character = char
    rootPart = char:WaitForChild("HumanoidRootPart")
    humanoid = char:WaitForChild("Humanoid")

    originalCollision = {}
    originalWalkSpeed = humanoid.WalkSpeed
    autoRotateWasEnabled = humanoid.AutoRotate

    if rootPart:FindFirstChild("FlightAttachment") then rootPart.FlightAttachment:Destroy() end
    if rootPart:FindFirstChild("FlightVelocity") then rootPart.FlightVelocity:Destroy() end
    if rootPart:FindFirstChild("SpeedAttachment") then rootPart.SpeedAttachment:Destroy() end
    if rootPart:FindFirstChild("SpeedVelocity") then rootPart.SpeedVelocity:Destroy() end

    attachment = Instance.new("Attachment", rootPart)
    attachment.Name = "FlightAttachment"

    linearVelocity = Instance.new("LinearVelocity", rootPart)
    linearVelocity.Name = "FlightVelocity"
    linearVelocity.Attachment0 = attachment
    linearVelocity.RelativeTo = Enum.ActuatorRelativeTo.World
    linearVelocity.MaxForce = 0
    linearVelocity.VectorVelocity = Vector3.zero

    speedAttachment = Instance.new("Attachment", rootPart)
    speedAttachment.Name = "SpeedAttachment"

    speedLinearVelocity = Instance.new("LinearVelocity", rootPart)
    speedLinearVelocity.Name = "SpeedVelocity"
    speedLinearVelocity.Attachment0 = speedAttachment
    speedLinearVelocity.RelativeTo = Enum.ActuatorRelativeTo.World
    speedLinearVelocity.MaxForce = 0
    speedLinearVelocity.VectorVelocity = Vector3.zero

    if not firstSpawnCaptured and rootPart then
        realSpawnCFrame = rootPart.CFrame
        firstSpawnCaptured = true
    end

    if noclipEnabled then applyNoclip(true) end
    watchCharacterForInvisibility(char)
    if invisibleEnabled then applyInvisibility(true) end
    if flyToggleEnabled then setFlying(true) end

    if promptExtenderEnabled then
        task.defer(function() enablePromptExtenderAll() end)
    end

    humanoid.Died:Connect(function()
        character = nil
        rootPart = nil
        humanoid = nil
        if invisConnection then invisConnection:Disconnect() invisConnection = nil end
    end)
end

task.spawn(function()
    setupCharacter(player.Character or player.CharacterAdded:Wait())
end)

player.CharacterAdded:Connect(function(c)
    isFlying = false
    isTPBusy = false
    originalTransparency = {}
    originalCollision = {}
    if linearVelocity then linearVelocity.MaxForce = 0 end
    setupCharacter(c)
end)
