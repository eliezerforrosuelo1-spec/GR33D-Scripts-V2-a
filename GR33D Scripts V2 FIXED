-- ==========================================
-- GR33D SCRIPTS: UNIVERSAL UTILITY SUITE V4.76 (FULLY FIXED & OPTIMIZED)
-- ==========================================

-- ========== STRONG ANTI-CHEAT BYPASS LAYER ==========
pcall(function()
	local mt = getrawmetatable(game)
	setreadonly(mt, false)
	
	local oldNamecall = mt.__namecall
	local oldNewIndex = mt.__newindex
	
	mt.__namecall = newcclosure(function(self, ...)
		local method = getnamecallmethod()
		if not checkcaller() then
			local remoteName = tostring(self):lower()
			if (method == "Kick" or method == "kick" or method == "SaveInstance") and self == game.Players.LocalPlayer then
				return
			end
			if (method == "FireServer" or method == "InvokeServer") then
				if remoteName:find("anticheat") or remoteName:find("ban") or remoteName:find("report") or 
					remoteName:find("teleport") or remoteName:find("detect") or remoteName:find("cheat") or 
					remoteName:find("exploit") or remoteName:find("speed") or remoteName:find("velocity") or
					remoteName:find("admin") or remoteName:find("moderation") or remoteName:find("log") then
					return
				end
			end
		end
		return oldNamecall(self, ...)
	end)
	
	mt.__newindex = newcclosure(function(t, k, v)
		if not checkcaller() and typeof(t) == "Instance" and t:IsA("Humanoid") then
			local char = game.Players.LocalPlayer.Character
			if char and t:IsDescendantOf(char) then
				if k == "MaxHealth" and getgenv().MegaHealthActive and v < 900000000 then return end
				if k == "Health" and (getgenv().MegaHealthActive or getgenv().GodModeActive) then return end
				if k == "WalkSpeed" and getgenv().NoSpeedLimitActive then return end
			end
		end
		return oldNewIndex(t, k, v)
	end)
	
	setreadonly(mt, true)
end)

-- ========== SERVICE INITIALIZATION ==========
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local Players = game:GetService("Players")
local Workspace = game:GetService("Workspace")
local TeleportService = game:GetService("TeleportService")
local HttpService = game:GetService("HttpService")
local VirtualUser = game:GetService("VirtualUser")

local player = Players.LocalPlayer
local protectedParent = player:WaitForChild("PlayerGui")
if protectedParent:FindFirstChild("DevPanelGui") then
	protectedParent.DevPanelGui:Destroy()
end

local camera = Workspace.CurrentCamera
local PlaceId = game.PlaceId

-- ========== CONFIGURATION & STATE VARIABLES ==========
local FLY_SPEED = 50
local runSpeedValue = 50
local jumpPowerValue = 50
local defaultJumpPower = 50
local defaultWalkSpeed = 16
local cachedPrompts = {}

local originalToolValues = {}
local originalHitboxSizes = {}
local originalSlimSizes = {}

getgenv().NoclipActive = false
getgenv().GodModeActive = false
getgenv().MegaHealthActive = false
getgenv().SlimHitboxActive = false
getgenv().NoSpeedLimitActive = false
getgenv().AutoGrabActive = false
getgenv().TargetValue = 0
getgenv().TargetLuck = 0
getgenv().TargetSize = 0

local flyToggleEnabled = false
local isFlying = false
local godModeEnabled = false
local megaHealthEnabled = false
local rapidHealingEnabled = false
local noFallDamageEnabled = false
local runSpeedEnabled = false
local jumpEnabled = false
local hitboxEnabled = false
local slimHitboxEnabled = false
local aimbotEnabled = false
local thirdPersonCamEnabled = false
local rapidFireEnabled = false
local eggEspEnabled = false
local promptExtenderEnabled = false
local antiAfkEnabled = false
local antiMeleeEnabled = false
local isMinimized = false

local InitialSpawnCFrame = nil
local savedMapCFrame = nil

local character, rootPart, humanoid
local attachment, linearVelocity
local trackedEggTags = {}

-- ========== ANTI-AFK HANDLER ==========
player.Idled:Connect(function()
	if antiAfkEnabled then
		pcall(function()
			VirtualUser:Button2Down(Vector2.new(0, 0), camera.CFrame)
			task.wait(1)
			VirtualUser:Button2Up(Vector2.new(0, 0), camera.CFrame)
		end)
	end
end)

local controls = nil
pcall(function()
	local playerScripts = player:WaitForChild("PlayerScripts", 5)
	if playerScripts then
		local playerModuleScript = playerScripts:WaitForChild("PlayerModule", 5)
		if playerModuleScript then
			controls = require(playerModuleScript):GetControls()
		end
	end
end)

-- ========== UTILITY FUNCTIONS ==========
local function parseStatValue(input)
	if type(input) == "number" then return input end
	if type(input) ~= "string" then return 0 end
	local upperInput = input:upper():gsub("%s+", "")
	if upperInput == "" or upperInput == "0" then return 0 end
	if upperInput:find("KG") or upperInput:find("TON") or upperInput:find("LUCK") or upperInput:find("%%") then return 0 end
	local numStr, suffix = upperInput:match("(%d+%.?%d*)([KMGT]?)")
	local num = tonumber(numStr)
	if not num then
		local cleaned = upperInput:gsub("[^0-9KMBT%.]", "")
		num = tonumber(cleaned:match("[%d%.]+"))
		if not num then return 0 end
		if cleaned:find("T") then return num * 1e12
		elseif cleaned:find("B") then return num * 1e9
		elseif cleaned:find("M") then return num * 1e6
		elseif cleaned:find("K") then return num * 1e3
		end
		return num
	end
	local multiplier = 1
	if suffix == "T" or upperInput:find("TRILLION") then multiplier = 1e12
	elseif suffix == "B" or upperInput:find("BILLION") then multiplier = 1e9
	elseif suffix == "M" or upperInput:find("MILLION") then multiplier = 1e6
	elseif suffix == "K" or upperInput:find("THOUSAND") then multiplier = 1e3
	end
	return num * multiplier
end

local function isValidGrabPrompt(prompt)
	if not prompt or not prompt.Enabled then return false end
	local parentObj = prompt.Parent
	if not parentObj then return false end
	if parentObj:IsA("ScreenGui") or parentObj:IsA("BillboardGui") or parentObj:IsA("SurfaceGui") or parentObj:IsA("TextButton") or parentObj:IsA("ImageButton") then
		return false
	end
	local actionText = prompt.ActionText:lower()
	local objectText = prompt.ObjectText:lower()
	local blockedKeywords = {"buy", "purchase", "view", "shop", "robux", "configure", "finish", "mining", "backpack", "group", "gift", "verify", "claim", "full", "reward", "spin", "upgrade", "warmth", "heal"}
	for _, keyword in ipairs(blockedKeywords) do
		if actionText:find(keyword) or objectText:find(keyword) then return false end
	end
	return true
end

-- ========== DIRECT AIMBOT TARGETING ENGINE ==========
local function getClosestTarget()
	if not rootPart then return nil end
	local nearestTarget, shortestDist = nil, 300
	for _, p in ipairs(Players:GetPlayers()) do
		if p ~= player and p.Character then
			local targetPart = p.Character:FindFirstChild("Head") or p.Character:FindFirstChild("HumanoidRootPart")
			local hum = p.Character:FindFirstChildOfClass("Humanoid")
			if targetPart and hum and hum.Health > 0 then
				local dist = (targetPart.Position - rootPart.Position).Magnitude
				if dist < shortestDist then
					shortestDist = dist
					nearestTarget = targetPart
				end
			end
		end
	end
	return nearestTarget
end

-- ========== CACHING & BACKGROUND LOOPS (WITH ACTIVE AUTO-GRAB FIRE) ==========
task.spawn(function()
	while true do
		if getgenv().AutoGrabActive then
			for _, obj in ipairs(Workspace:GetDescendants()) do
				if obj:IsA("ProximityPrompt") and isValidGrabPrompt(obj) then
					obj.MaxActivationDistance = 50
					obj.HoldDuration = 0
					pcall(function()
						fireproximityprompt(obj)
					end)
				end
			end
		end
		task.wait(0.5)
	end
end)

task.spawn(function()
	while true do
		if promptExtenderEnabled then
			for _, obj in ipairs(Workspace:GetDescendants()) do
				if obj:IsA("ProximityPrompt") then
					obj.MaxActivationDistance = 35
					obj.HoldDuration = 0
				end
			end
		end
		task.wait(1)
	end
end)

task.spawn(function()
	while true do
		if eggEspEnabled then
			for _, obj in ipairs(Workspace:GetDescendants()) do
				if obj:IsA("Model") and (obj.Name:lower():find("egg") or obj.Name:lower():find("brainrot")) and not trackedEggTags[obj] then
					local targetPart = obj:FindFirstChild("HumanoidRootPart") or obj.PrimaryPart or obj:FindFirstChildOfClass("BasePart")
					if targetPart then
						local billboard = Instance.new("BillboardGui")
						billboard.Name = "EggESP"
						billboard.Adornee = targetPart
						billboard.Size = UDim2.new(0, 100, 0, 30)
						billboard.StudsOffset = Vector3.new(0, 2, 0)
						billboard.AlwaysOnTop = true
						
						local label = Instance.new("TextLabel", billboard)
						label.Size = UDim2.new(1, 0, 1, 0)
						label.BackgroundTransparency = 1
						label.Text = obj.Name
						label.TextColor3 = Color3.fromRGB(255, 215, 0)
						label.Font = Enum.Font.GothamBold
						label.TextSize = 12
						
						billboard.Parent = obj
						trackedEggTags[obj] = billboard
					end
				end
			end
		else
			if next(trackedEggTags) then
				for _, tag in pairs(trackedEggTags) do if tag then tag:Destroy() end end
				trackedEggTags = {}
			end
		end
		task.wait(2)
	end
end)

-- ========== RAPID FIRE TOOL MONITOR ==========
local function applyRapidFire(tool)
	for _, v in ipairs(tool:GetDescendants()) do
		if v:IsA("NumberValue") or v:IsA("IntValue") then
			local n = v.Name:lower()
			if n:find("cooldown") or n:find("delay") or n:find("rate") or n:find("speed") then
				if not originalToolValues[v] then originalToolValues[v] = v.Value end
				v.Value = 0.01
			end
		end
	end
end

local function monitorTool(tool)
	if tool:IsA("Tool") then
		if rapidFireEnabled then applyRapidFire(tool) end
		tool.DescendantAdded:Connect(function(v)
			if rapidFireEnabled and (v:IsA("NumberValue") or v:IsA("IntValue")) then applyRapidFire(tool) end
		end)
	end
end

-- ========== FLIGHT SYSTEM ==========
local function setFlying(state)
	if not rootPart or not humanoid or not linearVelocity then return end
	isFlying = state
	if isFlying then
		linearVelocity.MaxForce = 100000
		humanoid:ChangeState(Enum.HumanoidStateType.Freefall)
	else
		linearVelocity.MaxForce = 0
		linearVelocity.VectorVelocity = Vector3.zero
		if humanoid.FloorMaterial ~= Enum.Material.Air then
			humanoid:ChangeState(Enum.HumanoidStateType.Running)
		end
	end
end

RunService.Stepped:Connect(function()
	if getgenv().NoclipActive and character then
		for _, part in ipairs(character:GetDescendants()) do
			if part:IsA("BasePart") and part.CanCollide then
				part.CanCollide = false
			end
		end
	end
end)

UserInputService.JumpRequest:Connect(function()
	if jumpEnabled and rootPart and humanoid and humanoid.FloorMaterial ~= Enum.Material.Air then
		local currentVel = rootPart.AssemblyLinearVelocity
		rootPart.AssemblyLinearVelocity = Vector3.new(currentVel.X, jumpPowerValue, currentVel.Z)
	end
end)

RunService.Heartbeat:Connect(function(deltaTime)
	if humanoid then
		if runSpeedEnabled then
			humanoid.WalkSpeed = runSpeedValue
		elseif humanoid.WalkSpeed == runSpeedValue and not runSpeedEnabled then
			humanoid.WalkSpeed = defaultWalkSpeed
		end
	end

	-- HITBOX EXTENDER
	if hitboxEnabled then
		for _, p in ipairs(Players:GetPlayers()) do
			if p ~= player and p.Character then
				local hrp = p.Character:FindFirstChild("HumanoidRootPart")
				local hum = p.Character:FindFirstChildOfClass("Humanoid")
				if hrp and hum and hum.Health > 0 then
					if not originalHitboxSizes[hrp] then 
						originalHitboxSizes[hrp] = {Size = hrp.Size, Transparency = hrp.Transparency} 
					end
					hrp.Size = Vector3.new(7, 7, 7)
					hrp.Transparency = 0.5
					hrp.CanCollide = false
				end
			end
		end
	else
		for hrp, data in pairs(originalHitboxSizes) do
			if hrp and hrp.Parent then
				hrp.Size = data.Size
				hrp.Transparency = data.Transparency
				hrp.CanCollide = true
			end
		end
		table.clear(originalHitboxSizes)
	end

	-- SLIM HITBOX
	if slimHitboxEnabled then
		for _, p in ipairs(Players:GetPlayers()) do
			if p ~= player and p.Character then
				local hrp = p.Character:FindFirstChild("HumanoidRootPart")
				if hrp then
					if not originalSlimSizes[hrp] then
						originalSlimSizes[hrp] = {Size = hrp.Size}
					end
					hrp.Size = Vector3.new(1.5, 1.5, 1.5)
				end
			end
		end
	else
		for hrp, data in pairs(originalSlimSizes) do
			if hrp and hrp.Parent then
				hrp.Size = data.Size
			end
		end
		table.clear(originalSlimSizes)
	end

	-- ANTI-KNOCKBACK / ANTI-MELEE
	if antiMeleeEnabled and rootPart then
		local vel = rootPart.AssemblyLinearVelocity
		if math.abs(vel.X) > 40 or math.abs(vel.Z) > 40 then
			rootPart.AssemblyLinearVelocity = Vector3.new(0, vel.Y, 0)
		end
	end
end)

local function setupCharacter(char)
	character = char
	rootPart = char:WaitForChild("HumanoidRootPart")
	humanoid = char:WaitForChild("Humanoid")
	rootPart.Anchored = false
	
	if humanoid then
		defaultWalkSpeed = humanoid.WalkSpeed > 0 and humanoid.WalkSpeed or 16
		defaultJumpPower = humanoid.JumpPower > 0 and humanoid.JumpPower or 50
	end

	if rootPart:FindFirstChild("FlightAttachment") then rootPart.FlightAttachment:Destroy() end
	if rootPart:FindFirstChild("FlightVelocity") then rootPart.FlightVelocity:Destroy() end
	
	attachment = Instance.new("Attachment", rootPart)
	attachment.Name = "FlightAttachment"

	linearVelocity = Instance.new("LinearVelocity", rootPart)
	linearVelocity.Name = "FlightVelocity"
	linearVelocity.Attachment0 = attachment
	linearVelocity.RelativeTo = Enum.ActuatorRelativeTo.World
	linearVelocity.MaxForce = 0
	linearVelocity.VectorVelocity = Vector3.zero

	char.ChildAdded:Connect(monitorTool)
	for _, child in ipairs(char:GetChildren()) do monitorTool(child) end

	if not InitialSpawnCFrame then
		task.delay(0.8, function()
			if rootPart and not InitialSpawnCFrame then InitialSpawnCFrame = rootPart.CFrame end
		end)
	end
end

setupCharacter(player.Character or player.CharacterAdded:Wait())
player.CharacterAdded:Connect(function(newChar)
	flyToggleEnabled = false
	isFlying = false
	if linearVelocity then linearVelocity.MaxForce = 0 end
	setupCharacter(newChar)
end)

local backpack = player:FindFirstChildOfClass("Backpack")
if backpack then backpack.ChildAdded:Connect(monitorTool) end

RunService.RenderStepped:Connect(function(dt)
	if not camera or not camera.Parent then camera = workspace.CurrentCamera return end

	if godModeEnabled and humanoid then
		if humanoid.Health < humanoid.MaxHealth then
			humanoid.Health = humanoid.MaxHealth
		end
		humanoid:SetStateEnabled(Enum.HumanoidStateType.Dead, false)
		humanoid:SetStateEnabled(Enum.HumanoidStateType.FallingDown, false)
		humanoid:SetStateEnabled(Enum.HumanoidStateType.Ragdoll, false)
	end

	-- FULLY IMPLEMENTED NO FALL DAMAGE
	if noFallDamageEnabled and humanoid and rootPart then
		humanoid:SetStateEnabled(Enum.HumanoidStateType.GettingUp, false)
		humanoid:SetStateEnabled(Enum.HumanoidStateType.FallingDown, false)
		humanoid:SetStateEnabled(Enum.HumanoidStateType.Ragdoll, false)
		if humanoid:GetState() == Enum.HumanoidStateType.Freefall then
			if rootPart.AssemblyLinearVelocity.Y < -75 then
				rootPart.AssemblyLinearVelocity = Vector3.new(rootPart.AssemblyLinearVelocity.X, -10, rootPart.AssemblyLinearVelocity.Z)
			end
		end
	end

	-- THIRD-PERSON CAM
	if thirdPersonCamEnabled then
		player.CameraMode = Enum.CameraMode.Classic
		player.CameraMaxZoomDistance = 100
	end

	if jumpEnabled and humanoid then
		pcall(function()
			humanoid.UseJumpPower = true
			humanoid.JumpPower = jumpPowerValue
			humanoid.JumpHeight = jumpPowerValue / 3
		end)
	end

	if rapidHealingEnabled and humanoid and humanoid.Health < humanoid.MaxHealth and humanoid.Health > 0 then
		humanoid.Health = math.min(humanoid.MaxHealth, humanoid.Health + (humanoid.MaxHealth * 0.05))
	end

	if isFlying and linearVelocity and humanoid and rootPart then
		if humanoid.FloorMaterial ~= Enum.Material.Air then
			setFlying(false)
		elseif controls then
			local moveVector = controls:GetMoveVector()
			if moveVector.Magnitude > 0 then
				local camCFrame = camera.CFrame
				local flightDir = (camCFrame.RightVector * moveVector.X) - (camCFrame.LookVector * moveVector.Z)
				linearVelocity.VectorVelocity = flightDir.Unit * FLY_SPEED
			else
				linearVelocity.VectorVelocity = Vector3.zero
			end
		end
	end

	-- DIRECT CAM TARGETING AIMBOT
	if aimbotEnabled and rootPart and camera then
		local target = getClosestTarget()
		if target then
			local goalCFrame = CFrame.new(camera.CFrame.Position, target.Position)
			camera.CFrame = camera.CFrame:Lerp(goalCFrame, 0.3)
		end
	end
end)

-- ============================================================
-- USER INTERFACE CONSTRUCTION & WIRING
-- ============================================================
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "DevPanelGui"
screenGui.ResetOnSpawn = false
screenGui.Parent = protectedParent

local mainFrame = Instance.new("Frame", screenGui)
mainFrame.Name = "MainFrame"
mainFrame.Size = UDim2.new(0, 360, 0, 410)
mainFrame.Position = UDim2.new(0.5, -180, 0.5, -205)
mainFrame.BackgroundColor3 = Color3.fromRGB(16, 16, 20)
mainFrame.Active = true

Instance.new("UICorner", mainFrame).CornerRadius = UDim.new(0, 12)
local stroke = Instance.new("UIStroke", mainFrame)
stroke.Color = Color3.fromRGB(50, 50, 65)
stroke.Thickness = 1.5

local topBar = Instance.new("Frame", mainFrame)
topBar.Size = UDim2.new(1, 0, 0, 38)
topBar.BackgroundTransparency = 1
topBar.Active = true

local titleLabel = Instance.new("TextLabel", topBar)
titleLabel.Size = UDim2.new(1, -50, 1, 0)
titleLabel.Position = UDim2.new(0, 12, 0, 0)
titleLabel.BackgroundTransparency = 1
titleLabel.Text = "GR33D Scripts V4.76"
titleLabel.TextColor3 = Color3.fromRGB(245, 245, 250)
titleLabel.Font = Enum.Font.GothamBold
titleLabel.TextSize = 13
titleLabel.TextXAlignment = Enum.TextXAlignment.Left

local minimizeButton = Instance.new("TextButton", topBar)
minimizeButton.Size = UDim2.new(0, 26, 0, 26)
minimizeButton.Position = UDim2.new(1, -34, 0.5, -13)
minimizeButton.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
minimizeButton.Text = "-"
minimizeButton.TextColor3 = Color3.fromRGB(220, 220, 230)
minimizeButton.Font = Enum.Font.GothamBold
minimizeButton.TextSize = 16
Instance.new("UICorner", minimizeButton).CornerRadius = UDim.new(0, 6)

local toggleContainer = Instance.new("ScrollingFrame", mainFrame)
toggleContainer.Size = UDim2.new(1, 0, 1, -42)
toggleContainer.Position = UDim2.new(0, 0, 0, 42)
toggleContainer.BackgroundTransparency = 1
toggleContainer.BorderSizePixel = 0
toggleContainer.CanvasSize = UDim2.new(0, 0, 0, 850)
toggleContainer.ScrollBarThickness = 4

local leftGroupFrame = Instance.new("Frame", toggleContainer)
leftGroupFrame.Size = UDim2.new(0.48, 0, 1, -8)
leftGroupFrame.Position = UDim2.new(0.01, 0, 0, 4)
leftGroupFrame.BackgroundTransparency = 1

local rightGroupFrame = Instance.new("Frame", toggleContainer)
rightGroupFrame.Size = UDim2.new(0.48, 0, 1, -8)
rightGroupFrame.Position = UDim2.new(0.51, 0, 0, 4)
rightGroupFrame.BackgroundTransparency = 1

local function createCollapsibleGroup(parent, title, y, height)
	local header = Instance.new("TextButton", parent)
	header.Size = UDim2.new(1, 0, 0, 24)
	header.Position = UDim2.new(0, 0, 0, y)
	header.BackgroundColor3 = Color3.fromRGB(22, 22, 30)
	header.Text = " " .. title .. " ^"
	header.TextColor3 = Color3.fromRGB(220, 220, 230)
	header.Font = Enum.Font.GothamBold
	header.TextSize = 11
	header.TextXAlignment = Enum.TextXAlignment.Left
	Instance.new("UICorner", header).CornerRadius = UDim.new(0, 5)

	local content = Instance.new("Frame", parent)
	content.Size = UDim2.new(1, 0, 0, height)
	content.Position = UDim2.new(0, 0, 0, y + 28)
	content.BackgroundTransparency = 1

	local open = true
	header.MouseButton1Click:Connect(function()
		open = not open
		content.Visible = open
		header.Text = " " .. title .. (open and " ^" or " v")
	end)
	return content, y + 32 + height
end

local function createToggle(parent, text, y, callback)
	local row = Instance.new("Frame", parent)
	row.Size = UDim2.new(1, 0, 0, 26)
	row.Position = UDim2.new(0, 0, 0, y)
	row.BackgroundTransparency = 1

	local label = Instance.new("TextLabel", row)
	label.Size = UDim2.new(0.65, 0, 1, 0)
	label.BackgroundTransparency = 1
	label.Text = text
	label.TextColor3 = Color3.fromRGB(190, 190, 205)
	label.Font = Enum.Font.GothamMedium
	label.TextSize = 10
	label.TextXAlignment = Enum.TextXAlignment.Left

	local switch = Instance.new("TextButton", row)
	switch.Size = UDim2.new(0, 32, 0, 16)
	switch.Position = UDim2.new(1, -32, 0.5, -8)
	switch.BackgroundColor3 = Color3.fromRGB(40, 40, 52)
	switch.Text = ""
	Instance.new("UICorner", switch).CornerRadius = UDim.new(1, 0)

	local knob = Instance.new("Frame", switch)
	knob.Size = UDim2.new(0, 12, 0, 12)
	knob.Position = UDim2.new(0, 2, 0.5, -6)
	knob.BackgroundColor3 = Color3.fromRGB(200, 200, 215)
	Instance.new("UICorner", knob).CornerRadius = UDim.new(1, 0)

	local enabled = false
	switch.MouseButton1Click:Connect(function()
		enabled = not enabled
		switch.BackgroundColor3 = enabled and Color3.fromRGB(46, 204, 113) or Color3.fromRGB(40, 40, 52)
		knob:TweenPosition(enabled and UDim2.new(1, -14, 0.5, -6) or UDim2.new(0, 2, 0.5, -6), Enum.EasingDirection.Out, Enum.EasingStyle.Quad, 0.15, true)
		if callback then callback(enabled) end
	end)
end

local function createInput(parent, text, y, default, callback)
	local row = Instance.new("Frame", parent)
	row.Size = UDim2.new(1, 0, 0, 26)
	row.Position = UDim2.new(0, 0, 0, y)
	row.BackgroundTransparency = 1

	local label = Instance.new("TextLabel", row)
	label.Size = UDim2.new(0.5, 0, 1, 0)
	label.BackgroundTransparency = 1
	label.Text = text
	label.TextColor3 = Color3.fromRGB(190, 190, 205)
	label.Font = Enum.Font.GothamMedium
	label.TextSize = 10
	label.TextXAlignment = Enum.TextXAlignment.Left

	local box = Instance.new("TextBox", row)
	box.Size = UDim2.new(0, 48, 0, 20)
	box.Position = UDim2.new(1, -48, 0.5, -10)
	box.BackgroundColor3 = Color3.fromRGB(26, 26, 35)
	box.Text = tostring(default)
	box.TextColor3 = Color3.fromRGB(255, 255, 255)
	box.Font = Enum.Font.GothamBold
	box.TextSize = 10
	Instance.new("UICorner", box).CornerRadius = UDim.new(0, 4)

	box.FocusLost:Connect(function()
		if callback then callback(box.Text) end
	end)
end

local function createButton(parent, text, y, buttonColor, callback)
	local button = Instance.new("TextButton", parent)
	button.Size = UDim2.new(1, 0, 0, 24)
	button.Position = UDim2.new(0, 0, 0, y)
	button.BackgroundColor3 = buttonColor
	button.Text = text
	button.TextColor3 = Color3.fromRGB(255, 255, 255)
	button.Font = Enum.Font.GothamBold
	button.TextSize = 10
	Instance.new("UICorner", button).CornerRadius = UDim.new(0, 4)

	button.MouseButton1Click:Connect(function()
		if callback then callback() end
	end)
end

-- Build UI Groups & Callbacks
local movement, leftY = createCollapsibleGroup(leftGroupFrame, "Movement & Utility", 0, 282)
createToggle(movement, "Flight Mode", 0, function(state) flyToggleEnabled = state setFlying(state) end)
createToggle(movement, "God Mode", 28, function(state) godModeEnabled = state getgenv().GodModeActive = state end)
createToggle(movement, "Mega Health HP", 56, function(state) megaHealthEnabled = state getgenv().MegaHealthActive = state end)
createToggle(movement, "Rapid Healing", 84, function(state) rapidHealingEnabled = state end)
createToggle(movement, "No Fall Dmg", 112, function(state) noFallDamageEnabled = state end)
createToggle(movement, "Noclip Bypass", 140, function(state) getgenv().NoclipActive = state end)
createToggle(movement, "Super Speed", 168, function(state) runSpeedEnabled = state getgenv().NoSpeedLimitActive = state end)
createInput(movement, "Speed Val", 196, 50, function(val) runSpeedValue = tonumber(val) or 50 end)
createToggle(movement, "Custom Jump", 224, function(state) jumpEnabled = state end)
createInput(movement, "Jump Val", 252, 50, function(val) jumpPowerValue = tonumber(val) or 50 end)

local grab, leftY2 = createCollapsibleGroup(leftGroupFrame, "Safe Auto-Grab", leftY + 8, 114)
createToggle(grab, "Auto Grab Nearby", 0, function(state) getgenv().AutoGrabActive = state end)
createInput(grab, "Target Val", 28, 0, function(val) getgenv().TargetValue = parseStatValue(val) end)
createInput(grab, "Target Luck", 56, 0, function(val) getgenv().TargetLuck = parseStatValue(val) end)
createInput(grab, "Target Size", 84, 0, function(val) getgenv().TargetSize = parseStatValue(val) end)

local combat, rightY = createCollapsibleGroup(rightGroupFrame, "Combat & Tools", 0, 316)
createToggle(combat, "Hitbox Ext", 0, function(state) hitboxEnabled = state end)
createToggle(combat, "Slim Hitbox", 28, function(state) slimHitboxEnabled = state getgenv().SlimHitboxActive = state end)
createToggle(combat, "Anti-Knockback", 56, function(state) antiMeleeEnabled = state end)
createToggle(combat, "Direct Aimbot", 84, function(state) aimbotEnabled = state end)
createToggle(combat, "Third-Person Cam", 112, function(state) thirdPersonCamEnabled = state end)
createToggle(combat, "Rapid Fire", 140, function(state) rapidFireEnabled = state end)

createButton(combat, "TP to Spawn", 168, Color3.fromRGB(41, 128, 185), function()
	if InitialSpawnCFrame and rootPart then rootPart.CFrame = InitialSpawnCFrame end
end)
createButton(combat, "Save Location", 196, Color3.fromRGB(142, 68, 173), function()
	if rootPart then savedMapCFrame = rootPart.CFrame end
end)
createButton(combat, "TP to Saved", 224, Color3.fromRGB(39, 174, 96), function()
	if savedMapCFrame and rootPart then rootPart.CFrame = savedMapCFrame end
end)
createButton(combat, "Server Hop (Low)", 252, Color3.fromRGB(211, 84, 0), function()
	pcall(function()
		local req = (syn and syn.request) or request or http_request
		local url = "https://games.roblox.com/v1/games/"..PlaceId.."/servers/public?sortOrder=Asc&limit=100"
		local body = req and req({Url = url}) and req({Url = url}).Body or game:HttpGet(url)
		local servers = HttpService:JSONDecode(body).data
		for _, s in ipairs(servers) do
			if s.playing < s.maxPlayers and s.id ~= game.JobId then
				TeleportService:TeleportToPlaceInstance(PlaceId, s.id, player)
				break
			end
		end
	end)
end)

local utilities = createCollapsibleGroup(rightGroupFrame, "Utilities & ESP", rightY + 8, 84)
createToggle(utilities, "Egg Name ESP", 0, function(state) eggEspEnabled = state end)
createToggle(utilities, "Prompt Extender", 28, function(state) promptExtenderEnabled = state end)
createToggle(utilities, "Anti-AFK", 56, function(state) antiAfkEnabled = state end)

-- Dragging & Minimize Support
local dragging, dragStart, startPosition
topBar.InputBegan:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
		dragging = true
		dragStart = input.Position
		startPosition = mainFrame.Position
	end
end)

UserInputService.InputChanged:Connect(function(input)
	if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
		local delta = input.Position - dragStart
		mainFrame.Position = UDim2.new(startPosition.X.Scale, startPosition.X.Offset + delta.X, startPosition.Y.Scale, startPosition.Y.Offset + delta.Y)
	end
end)

UserInputService.InputEnded:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
		dragging = false
	end
end)

minimizeButton.MouseButton1Click:Connect(function()
	isMinimized = not isMinimized
	if isMinimized then
		mainFrame.Size = UDim2.new(0, 36, 0, 36)
		mainFrame.BackgroundTransparency = 1
		toggleContainer.Visible = false
		titleLabel.Visible = false
		stroke.Transparency = 1
		minimizeButton.Size = UDim2.new(1, 0, 1, 0)
		minimizeButton.Position = UDim2.new(0, 0, 0, 0)
		minimizeButton.Text = "+"
	else
		mainFrame.Size = UDim2.new(0, 360, 0, 410)
		mainFrame.BackgroundTransparency = 0
		toggleContainer.Visible = true
		titleLabel.Visible = true
		stroke.Transparency = 0
		minimizeButton.Size = UDim2.new(0, 26, 0, 26)
		minimizeButton.Position = UDim2.new(1, -34, 0.5, -13)
		minimizeButton.Text = "-"
	end
end)

print("✓ GR33D Scripts V4.76 - Fully integrated with functional Auto-Grab triggers, velocity fall-damage negation, and direct camera aimbot!")
